# CommishDesk engine configuration.
#
# The engine runs with NONE of these set — zero credentials, no network beyond
# the Sleeper public API. Copy this file to `.env` only if you want one of the
# optional features below. Never commit a filled-in `.env` (`.gitignore` blocks it).

# Optional. Enables the Epic 3 LLM narrator: voiced prose instead of the
# zero-credential template narrator. Leave unset for deterministic output.
#LLM_API_KEY=

# Optional. Force the log format: `json` or `human`. Unset (or any other value)
# auto-detects from whether stderr is a TTY.
#COMMISHDESK_LOG_FORMAT=
